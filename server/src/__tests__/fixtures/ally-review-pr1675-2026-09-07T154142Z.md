## Ally — Consolidated PR Review

_Lenses: pr-review-toolkit (code, tests, comments, errors, types) + gstack/review + native-codex._
Reviewed head: 583085ded80ad1ba6420744000a94fc8671c418b (unchanged since my last pass — re-review requested to disposition findings)

Same head, so no new diff to read. This pass is therefore about the three dispositions, and
**two of them are corrections to me, not to the PR.** I checked both rather than accepting them,
and re-derived the PR's central claim from scratch at this head so this review stands on its own
evidence rather than carrying the last one's.

### Prior Findings Dispositioned (2)

- **prior:583085ded important 1** — **fixed, and my finding was stale when I filed it.** Confirmed
  from the timeline, not the PR body:

  | event | time |
  |---|---|
  | retitle `docs(agents):` → `fix(attribution):` | `2026-09-07T03:42:24Z` |
  | my review submitted | `2026-09-07T03:46:19Z` |

  The retitle landed **3m55s before** I submitted. Live title is now
  `fix(attribution): correct stale git-identity guidance and de-vacuate the gate's tests`, head
  unchanged. My review sampled the title during composition and reported it as current at submit —
  that is a defect in how I run a long pass, not a wrong reading of the repo, and I have recorded it
  so I re-check mutable PR metadata (title, labels, merge state) at submit time rather than compose
  time. The finding is closed.

  Worth keeping from it: the diagnosis the author quotes back is the one that survives. `pr.yml`
  selects gates by changed path, not title, so `commitperclip`'s stated rationale was wrong; the
  real cost was that a squash-merge carries the title into `master`, filing a behavioural change to
  a CI gate's diagnostics as documentation. That cost is now avoided.

- **prior:583085ded recommended-action 4 (`update-branch`)** — **withdrawn. I was wrong, and the
  author is right that it would have been actively destructive.** Verified independently:

  ```
  mergeQueue(branch:"master").configuration.mergeMethod  ->  REBASE
  pr#1675: mergeable=true  mergeable_state=behind  rebaseable=true  commits=3
  git log --pretty='%h %p'  ->  583085d 581e8e5 / 581e8e5 c1337f0 / c1337f0 66d67f1   (all single-parent)
  ```

  `update-branch` performs a *merge*; on a REBASE queue a branch carrying merge commits reads
  `mergeable` but is not rebasable, and the recorded harm on this repo is not hypothetical:

  - **BLO-22647** (critical, done) — "GitHub App update-branch sync leaves required PR checks permanently expected"
  - **BLO-32428** — "Re-apply PR #1195 … onto master as a linear branch — 745 behind, **2 merge commits**"
  - **BLO-32429** — "**Linearize** PR #1279 and take ownership of the three conflict resolutions Ally authored in **merge commit** `ed438e347`"

  That last one is mine. So I am the agent who has already caused this exact harm once and then
  recommended the mechanism that causes it — which is a good argument for the author's instinct to
  record it rather than just skip it. `behind` needs no action here: the queue rebases onto live
  master itself, and `rebaseable=true` with three single-parent commits is exactly the shape that
  survives that. I have written this to memory so I stop re-deriving "it's behind, update it."

  **One correction to the record, since it is meant to be durable.** `BLO-22300` is the wrong cite
  for the wedging mechanism. Its own description is a *governance* gap — `security-review`/`review`
  never executing against the rebased landing commit — and it says so explicitly: its checks are
  "confirmed **not required** … so this does not stall or evict queue entries — it is a governance
  gap, not a throughput one." It does confirm the `REBASE` method, and its monitor notes do show
  `#1077` queue-position and eviction watching, so the association isn't baseless. But an agent who
  follows `BLO-22300` looking for "merge commit ⇒ dequeued before any build" will not find it there.
  The mechanism is carried by **BLO-22647** plus the two linearization tickets above. Worth fixing
  in whatever durable note this lands in, because a correct warning under a ticket number that
  contradicts it is the kind of thing that gets the warning discarded.

### Suggestion declines — accepted

- **`makeGit` hoist** — agreed, and the reasoning is better than mine. I classified it opportunistic
  and non-blocking myself, so the head-move cost decides it: a push dismisses the at-head review
  attestation the findings gate needs and restarts a ~38-minute suite, which is the wrong trade for
  a test-only readability gain on an otherwise-ready PR. Agreed on not filing a row for ~25 lines of
  test dedup either. Not re-raised.
- The `scripts/check-commit-author-attribution.mjs:464` precondition clause (my other Suggestion)
  wasn't addressed individually, and I'm not re-raising it — it was non-blocking, it falls under the
  same head-move argument, and the recommended diagnostic (`git log -1`) is correct in both worlds.

### Re-verified at this head (fresh, not carried forward)

The central claim, re-derived in my own overlay-bearing pod:

```
GIT_AUTHOR_EMAIL=ally@paperclip.blockcast.net   (overlay live)
git config --global user.email  ->  rc=1, unset
fresh `git init` + commit      ->  author Ally <ally@…> | committer Ally <ally@…>

head fixtures, overlay present :  28 tests | 28 pass | 0 fail
head fixtures, overlay stripped:  28 tests | 28 pass | 0 fail   (env -u GIT_AUTHOR_* GIT_COMMITTER_*)
base fixtures, overlay present :  28 tests | 24 pass | 4 FAIL
```

So the fixtures were genuinely broken in an agent pod, this fixes them, and the fix is correct in
both environments rather than branching on which is running. That contrast — not the "28/28" line —
is the load-bearing evidence, exactly as the PR's own Risks section says.

### Critical Issues (0)

### Important Issues (0)

### Suggestions (0)

Nothing new at this head, and nothing carried.

### On the one open gate

`General tests (server 3/4)` is **re-running as I write** (started `15:28:17Z`); the other 18
check-runs at this head are `success` or `skipped`, and the only *failing* statuses are my own two
gates, which this review is here to clear. I won't assert flake either — but the part a reviewer can
actually settle is causal independence, and that is structural rather than probabilistic:

- the diff is three files (`AGENTS.md`, `scripts/check-commit-author-attribution{,.test}.mjs`) — **nothing under `server/`**
- the only reference from `server/` to the changed script is a *comment* (`git-checkout-identity.ts:71`), not an import, so there is no path by which this diff reaches `server/src/__tests__/productivity-review-service.test.ts`
- that test contains no `GIT_AUTHOR*`/`GIT_COMMITTER*`/`git config`/attribution reference at all
- the lane is `success` on 10 of the last 12 open PRs, so it isn't systemically broken
- the reported signature — `Failed query: TRUNCATE TABLE "companies" CASCADE` in a fixture hook, at 259s — is DB contention in setup, not an assertion about behaviour this diff changes

Agreed that `verify` is only the aggregator, so that is one failure and not two.

### Strengths

- **The pushback was right on the substance and better-sourced than my recommendation.** Declining
  `update-branch` was correct, and naming it as worth recording rather than silently skipping is the
  right call precisely because it is the safe-looking move — I recommended it, and I had previously
  authored one of the merge commits that had to be linearized.
- The stale-finding argument was made from the timeline with timestamps, and it distinguishes
  "stale" from "wrong" instead of using the retitle to dismiss the category error. That distinction
  is the accurate one, and it kept the fix.
- Declining a cleanup on head-move cost — dismissed attestation plus a 38-minute suite against ~25
  lines of test dedup — is the correct reading of a non-blocking suggestion on a ready PR, and it
  resists the pull to touch the diff just because a reviewer mentioned it.
- The flake claim is held at "evidence points that way, I took a control rather than asserting it,"
  which is the right epistemic level for an unrelated lane failure.
- The change remains its own best worked example: the fixtures' identity mechanism was subject to
  the very precedence rule the docs half documents, and four tests were failing with two silently
  vacuous in every agent pod.

### Recommended Action

1. No Critical or Important issues. Nothing from my side blocks merge.
2. **Do not `update-branch`.** `behind` is fine on this repo's REBASE queue; the branch is
   `rebaseable=true` and all-single-parent, which is the shape that lands.
3. When recording the update-branch lesson, cite **BLO-22647** (+ BLO-32428/BLO-32429) rather than
   BLO-22300 — BLO-22300's own text says it does not evict queue entries.
4. Only open item is `General tests (server 3/4)`, re-running now and causally unreachable from this
   diff. If the re-run is green, merge; if it fails identically, it is still not this PR, but a flake
   row against that suite would be worth more than another re-run.

