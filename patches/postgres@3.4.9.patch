diff --git a/cf/src/connection.js b/cf/src/connection.js
index 8e79170aeb13efe8c3c77ccede0cd3a115b5e1b5..cdd36b6594d9fdf986556da4514a44e0a44f955a 100644
--- a/cf/src/connection.js
+++ b/cf/src/connection.js
@@ -77,6 +77,7 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
       , idleTimer = timer(end, options.idle_timeout)
       , lifeTimer = timer(end, options.max_lifetime)
       , connectTimer = timer(connectTimedOut, options.connect_timeout)
+      , closeTimer = timer(closeTimedOut, options.close_timeout)
 
   let socket = null
     , cancelMessage
@@ -254,6 +255,17 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
   }
 
   function nextWrite(fn) {
+    // BLO-19583 / porsager/postgres#1154: closed() nulls `socket`, but a write
+    // queued afterwards still schedules setImmediate(nextWrite). The immediate
+    // then dereferenced a null socket and threw *outside* any try/catch, taking
+    // the whole process down. Guard it, and fail the affected query through the
+    // driver's own connection-closed path so it rejects instead of hanging.
+    if (socket === null) {
+      nextWriteTimer !== null && clearImmediate(nextWriteTimer)
+      chunk = nextWriteTimer = null
+      error(Errors.connection('CONNECTION_CLOSED', options, socket))
+      return false
+    }
     const x = socket.write(chunk, fn)
     nextWriteTimer !== null && clearImmediate(nextWriteTimer)
     chunk = nextWriteTimer = null
@@ -417,15 +429,40 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
       !connection.reserved && onend(connection),
       !connection.reserved && !initial && !query && sent.length === 0
         ? (terminate(), new Promise(r => socket && socket.readyState !== 'closed' ? socket.once('close', r) : r()))
-        : ending = new Promise(r => ended = r)
+        : (closeTimer.start(), ending = new Promise(r => ended = r))
     )
   }
 
+  // BLO-35946: `end()` moves the connection onto the pool's terminal `ended`
+  // queue *before* the line above checks whether it is quiescent, and
+  // `handler()` never draws from `ended`. So the branch that waits for an
+  // in-flight query took the connection out of the pool and then depended on
+  // that query completing to reach `terminate()` -- the only thing that
+  // resolves `ended` and lets `closed()` -> `onclose` hand the slot back.
+  //
+  // A query that never completes therefore leaked the slot until the process
+  // restarted, leaving the server-side backend orphaned in `active`/
+  // `ClientRead` where no `statement_timeout`, no
+  // `idle_in_transaction_session_timeout` and no TCP keepalive can reap it.
+  // On 2026-09-24 that took 9 of 10 slots on paperclip-0 and stalled
+  // company-wide dispatch for 2h15m; recovery needed `pg_terminate_backend`.
+  //
+  // `close_timeout` bounds that wait. `terminate()` fails the stuck query
+  // through the driver's own CONNECTION_DESTROYED path and resolves `ended`;
+  // `destroy()` then guarantees the 'close' event, because `terminate()` only
+  // half-closes (`socket.end`) and a FIN is not enough on a socket whose peer
+  // is wedged mid-protocol. `closed()` returns the slot via `onclose`.
+  function closeTimedOut() {
+    terminate()
+    socket && socket.destroy()
+  }
+
   function terminate() {
     terminated = true
     if (stream || query || initial || sent.length)
       error(Errors.connection('CONNECTION_DESTROYED', options))
 
+    closeTimer.cancel()
     clearImmediate(nextWriteTimer)
     if (socket) {
       socket.removeListener('data', data)
@@ -442,6 +479,7 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
     clearImmediate(nextWriteTimer)
     socket.removeListener('data', data)
     socket.removeListener('connect', connected)
+    closeTimer.cancel()
     idleTimer.cancel()
     lifeTimer.cancel()
     connectTimer.cancel()
diff --git a/cf/src/index.js b/cf/src/index.js
index ffbe7aef075d6037c540cbd5e8f7e298d4345cc3..2560ab4662167edcea7fe001d06fbdf75ee8ec72 100644
--- a/cf/src/index.js
+++ b/cf/src/index.js
@@ -75,6 +75,18 @@ function Postgres(a, b) {
     END: CLOSE,
     PostgresError,
     options,
+    // BLO-33243: the pool's own queue lengths, so a saturated pool is
+    // observable instead of inferred. postgres.js keeps every connection in
+    // exactly one of these queues and `queries` is the backlog of statements
+    // with no connection yet, which is the saturation signal itself. Read-only
+    // snapshot of numbers -- the queues themselves stay closed over.
+    poolStats: () => ({
+      max: options.max,
+      idle: open.length,
+      active: busy.length + full.length + reserved.length,
+      connecting: connecting.length,
+      waiting: queries.length
+    }),
     reserve,
     listen,
     begin,
@@ -445,13 +457,18 @@ function parseOptions(a, b) {
   'timeout' in o && (console.log('The timeout option is deprecated, use idle_timeout instead'), o.idle_timeout = o.timeout) // eslint-disable-line
   query.sslrootcert === 'system' && (query.ssl = 'verify-full')
 
-  const ints = ['idle_timeout', 'connect_timeout', 'max_lifetime', 'max_pipeline', 'backoff', 'keep_alive']
+  const ints = ['idle_timeout', 'connect_timeout', 'close_timeout', 'max_lifetime', 'max_pipeline', 'backoff', 'keep_alive']
   const defaults = {
     max             : globalThis.Cloudflare ? 3 : 10,
     ssl             : false,
     sslnegotiation  : null,
     idle_timeout    : null,
     connect_timeout : 30,
+    // BLO-35946: how long `Connection.end()` waits for an in-flight query
+    // before terminating the connection instead of waiting forever. The
+    // connection is already off the pool by the time this starts, so an
+    // unbounded wait leaks the slot. See closeTimedOut() in connection.js.
+    close_timeout   : 30,
     max_lifetime    : max_lifetime,
     max_pipeline    : 100,
     backoff         : backoff,
diff --git a/cjs/src/connection.js b/cjs/src/connection.js
index 07f6716702ac888215c86ad6e0071aaf55ae519c..d919e6caca7e425823a2cdbd29df69d3af8a6488 100644
--- a/cjs/src/connection.js
+++ b/cjs/src/connection.js
@@ -75,6 +75,7 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
       , idleTimer = timer(end, options.idle_timeout)
       , lifeTimer = timer(end, options.max_lifetime)
       , connectTimer = timer(connectTimedOut, options.connect_timeout)
+      , closeTimer = timer(closeTimedOut, options.close_timeout)
 
   let socket = null
     , cancelMessage
@@ -252,6 +253,17 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
   }
 
   function nextWrite(fn) {
+    // BLO-19583 / porsager/postgres#1154: closed() nulls `socket`, but a write
+    // queued afterwards still schedules setImmediate(nextWrite). The immediate
+    // then dereferenced a null socket and threw *outside* any try/catch, taking
+    // the whole process down. Guard it, and fail the affected query through the
+    // driver's own connection-closed path so it rejects instead of hanging.
+    if (socket === null) {
+      nextWriteTimer !== null && clearImmediate(nextWriteTimer)
+      chunk = nextWriteTimer = null
+      error(Errors.connection('CONNECTION_CLOSED', options, socket))
+      return false
+    }
     const x = socket.write(chunk, fn)
     nextWriteTimer !== null && clearImmediate(nextWriteTimer)
     chunk = nextWriteTimer = null
@@ -415,15 +427,40 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
       !connection.reserved && onend(connection),
       !connection.reserved && !initial && !query && sent.length === 0
         ? (terminate(), new Promise(r => socket && socket.readyState !== 'closed' ? socket.once('close', r) : r()))
-        : ending = new Promise(r => ended = r)
+        : (closeTimer.start(), ending = new Promise(r => ended = r))
     )
   }
 
+  // BLO-35946: `end()` moves the connection onto the pool's terminal `ended`
+  // queue *before* the line above checks whether it is quiescent, and
+  // `handler()` never draws from `ended`. So the branch that waits for an
+  // in-flight query took the connection out of the pool and then depended on
+  // that query completing to reach `terminate()` -- the only thing that
+  // resolves `ended` and lets `closed()` -> `onclose` hand the slot back.
+  //
+  // A query that never completes therefore leaked the slot until the process
+  // restarted, leaving the server-side backend orphaned in `active`/
+  // `ClientRead` where no `statement_timeout`, no
+  // `idle_in_transaction_session_timeout` and no TCP keepalive can reap it.
+  // On 2026-09-24 that took 9 of 10 slots on paperclip-0 and stalled
+  // company-wide dispatch for 2h15m; recovery needed `pg_terminate_backend`.
+  //
+  // `close_timeout` bounds that wait. `terminate()` fails the stuck query
+  // through the driver's own CONNECTION_DESTROYED path and resolves `ended`;
+  // `destroy()` then guarantees the 'close' event, because `terminate()` only
+  // half-closes (`socket.end`) and a FIN is not enough on a socket whose peer
+  // is wedged mid-protocol. `closed()` returns the slot via `onclose`.
+  function closeTimedOut() {
+    terminate()
+    socket && socket.destroy()
+  }
+
   function terminate() {
     terminated = true
     if (stream || query || initial || sent.length)
       error(Errors.connection('CONNECTION_DESTROYED', options))
 
+    closeTimer.cancel()
     clearImmediate(nextWriteTimer)
     if (socket) {
       socket.removeListener('data', data)
@@ -440,6 +477,7 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
     clearImmediate(nextWriteTimer)
     socket.removeListener('data', data)
     socket.removeListener('connect', connected)
+    closeTimer.cancel()
     idleTimer.cancel()
     lifeTimer.cancel()
     connectTimer.cancel()
diff --git a/cjs/src/index.js b/cjs/src/index.js
index f09c61c7e998f411560b95c9a8e1ed6a97972c11..6d92cce05dee675855eba5b48f206d9f8c387b67 100644
--- a/cjs/src/index.js
+++ b/cjs/src/index.js
@@ -74,6 +74,18 @@ function Postgres(a, b) {
     END: CLOSE,
     PostgresError,
     options,
+    // BLO-33243: the pool's own queue lengths, so a saturated pool is
+    // observable instead of inferred. postgres.js keeps every connection in
+    // exactly one of these queues and `queries` is the backlog of statements
+    // with no connection yet, which is the saturation signal itself. Read-only
+    // snapshot of numbers -- the queues themselves stay closed over.
+    poolStats: () => ({
+      max: options.max,
+      idle: open.length,
+      active: busy.length + full.length + reserved.length,
+      connecting: connecting.length,
+      waiting: queries.length
+    }),
     reserve,
     listen,
     begin,
@@ -444,13 +456,18 @@ function parseOptions(a, b) {
   'timeout' in o && (console.log('The timeout option is deprecated, use idle_timeout instead'), o.idle_timeout = o.timeout) // eslint-disable-line
   query.sslrootcert === 'system' && (query.ssl = 'verify-full')
 
-  const ints = ['idle_timeout', 'connect_timeout', 'max_lifetime', 'max_pipeline', 'backoff', 'keep_alive']
+  const ints = ['idle_timeout', 'connect_timeout', 'close_timeout', 'max_lifetime', 'max_pipeline', 'backoff', 'keep_alive']
   const defaults = {
     max             : globalThis.Cloudflare ? 3 : 10,
     ssl             : false,
     sslnegotiation  : null,
     idle_timeout    : null,
     connect_timeout : 30,
+    // BLO-35946: how long `Connection.end()` waits for an in-flight query
+    // before terminating the connection instead of waiting forever. The
+    // connection is already off the pool by the time this starts, so an
+    // unbounded wait leaks the slot. See closeTimedOut() in connection.js.
+    close_timeout   : 30,
     max_lifetime    : max_lifetime,
     max_pipeline    : 100,
     backoff         : backoff,
diff --git a/src/connection.js b/src/connection.js
index 1b1cccde43b4570d5d071d6ffaab2669b3c2065a..50e6a362989e1359cfabead2427e4e5eea62e052 100644
--- a/src/connection.js
+++ b/src/connection.js
@@ -75,6 +75,7 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
       , idleTimer = timer(end, options.idle_timeout)
       , lifeTimer = timer(end, options.max_lifetime)
       , connectTimer = timer(connectTimedOut, options.connect_timeout)
+      , closeTimer = timer(closeTimedOut, options.close_timeout)
 
   let socket = null
     , cancelMessage
@@ -252,6 +253,17 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
   }
 
   function nextWrite(fn) {
+    // BLO-19583 / porsager/postgres#1154: closed() nulls `socket`, but a write
+    // queued afterwards still schedules setImmediate(nextWrite). The immediate
+    // then dereferenced a null socket and threw *outside* any try/catch, taking
+    // the whole process down. Guard it, and fail the affected query through the
+    // driver's own connection-closed path so it rejects instead of hanging.
+    if (socket === null) {
+      nextWriteTimer !== null && clearImmediate(nextWriteTimer)
+      chunk = nextWriteTimer = null
+      error(Errors.connection('CONNECTION_CLOSED', options, socket))
+      return false
+    }
     const x = socket.write(chunk, fn)
     nextWriteTimer !== null && clearImmediate(nextWriteTimer)
     chunk = nextWriteTimer = null
@@ -415,15 +427,40 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
       !connection.reserved && onend(connection),
       !connection.reserved && !initial && !query && sent.length === 0
         ? (terminate(), new Promise(r => socket && socket.readyState !== 'closed' ? socket.once('close', r) : r()))
-        : ending = new Promise(r => ended = r)
+        : (closeTimer.start(), ending = new Promise(r => ended = r))
     )
   }
 
+  // BLO-35946: `end()` moves the connection onto the pool's terminal `ended`
+  // queue *before* the line above checks whether it is quiescent, and
+  // `handler()` never draws from `ended`. So the branch that waits for an
+  // in-flight query took the connection out of the pool and then depended on
+  // that query completing to reach `terminate()` -- the only thing that
+  // resolves `ended` and lets `closed()` -> `onclose` hand the slot back.
+  //
+  // A query that never completes therefore leaked the slot until the process
+  // restarted, leaving the server-side backend orphaned in `active`/
+  // `ClientRead` where no `statement_timeout`, no
+  // `idle_in_transaction_session_timeout` and no TCP keepalive can reap it.
+  // On 2026-09-24 that took 9 of 10 slots on paperclip-0 and stalled
+  // company-wide dispatch for 2h15m; recovery needed `pg_terminate_backend`.
+  //
+  // `close_timeout` bounds that wait. `terminate()` fails the stuck query
+  // through the driver's own CONNECTION_DESTROYED path and resolves `ended`;
+  // `destroy()` then guarantees the 'close' event, because `terminate()` only
+  // half-closes (`socket.end`) and a FIN is not enough on a socket whose peer
+  // is wedged mid-protocol. `closed()` returns the slot via `onclose`.
+  function closeTimedOut() {
+    terminate()
+    socket && socket.destroy()
+  }
+
   function terminate() {
     terminated = true
     if (stream || query || initial || sent.length)
       error(Errors.connection('CONNECTION_DESTROYED', options))
 
+    closeTimer.cancel()
     clearImmediate(nextWriteTimer)
     if (socket) {
       socket.removeListener('data', data)
@@ -440,6 +477,7 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
     clearImmediate(nextWriteTimer)
     socket.removeListener('data', data)
     socket.removeListener('connect', connected)
+    closeTimer.cancel()
     idleTimer.cancel()
     lifeTimer.cancel()
     connectTimer.cancel()
diff --git a/src/index.js b/src/index.js
index c7fba3dace20173f4cb13f3f138bb6d213b43683..29b30c82994173104576aec430c4e95ba3de6537 100644
--- a/src/index.js
+++ b/src/index.js
@@ -74,6 +74,18 @@ function Postgres(a, b) {
     END: CLOSE,
     PostgresError,
     options,
+    // BLO-33243: the pool's own queue lengths, so a saturated pool is
+    // observable instead of inferred. postgres.js keeps every connection in
+    // exactly one of these queues and `queries` is the backlog of statements
+    // with no connection yet, which is the saturation signal itself. Read-only
+    // snapshot of numbers -- the queues themselves stay closed over.
+    poolStats: () => ({
+      max: options.max,
+      idle: open.length,
+      active: busy.length + full.length + reserved.length,
+      connecting: connecting.length,
+      waiting: queries.length
+    }),
     reserve,
     listen,
     begin,
@@ -444,13 +456,18 @@ function parseOptions(a, b) {
   'timeout' in o && (console.log('The timeout option is deprecated, use idle_timeout instead'), o.idle_timeout = o.timeout) // eslint-disable-line
   query.sslrootcert === 'system' && (query.ssl = 'verify-full')
 
-  const ints = ['idle_timeout', 'connect_timeout', 'max_lifetime', 'max_pipeline', 'backoff', 'keep_alive']
+  const ints = ['idle_timeout', 'connect_timeout', 'close_timeout', 'max_lifetime', 'max_pipeline', 'backoff', 'keep_alive']
   const defaults = {
     max             : globalThis.Cloudflare ? 3 : 10,
     ssl             : false,
     sslnegotiation  : null,
     idle_timeout    : null,
     connect_timeout : 30,
+    // BLO-35946: how long `Connection.end()` waits for an in-flight query
+    // before terminating the connection instead of waiting forever. The
+    // connection is already off the pool by the time this starts, so an
+    // unbounded wait leaks the slot. See closeTimedOut() in connection.js.
+    close_timeout   : 30,
     max_lifetime    : max_lifetime,
     max_pipeline    : 100,
     backoff         : backoff,
